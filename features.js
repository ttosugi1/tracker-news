function story_ownership_count(raw) {
  const me_id = raw.me.id;

  let count = 0;
  raw.iterations[project_id];
}

module.exports = {
  generate: function(raw) {
    return {
      raw: raw,
      derived: {
        story_ownership_count: story_ownership_count(rawData);
      }
    }
  }
}
